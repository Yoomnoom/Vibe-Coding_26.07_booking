# 수업 인수인계 문서

작성일: 2026-07-20

이 문서는 학원에서 설정한 내용을 집 컴퓨터에서도 다시 따라 하기 위한 메모입니다.
나는 초보자 기준으로 진행하므로, 집에서는 아래 순서대로 설치하면 됩니다.

## 1. 오늘 학원 PC에 설치한 것

### Codex skill

학원 PC의 사용자 전역 폴더에 설치됨:

```powershell
C:\Users\user\.codex\skills
```

설치된 항목:

- `find-skills`
- `ponytail`

### Agent skills

Matt Pocock skills는 아래 폴더에 설치됨:

```powershell
C:\Users\user\.agents\skills
```

설치된 주요 항목:

- `ask-matt`
- `code-review`
- `codebase-design`
- `diagnosing-bugs`
- `domain-modeling`
- `grill-me`
- `grill-with-docs`
- `implement`
- `improve-codebase-architecture`
- `prototype`
- `research`
- `resolving-merge-conflicts`
- `setup-matt-pocock-skills`
- `tdd`
- `to-spec`
- `to-tickets`
- `triage`
- `wayfinder`

그 외 writing, QA, refactor, pre-commit 관련 skill까지 총 41개가 설치됨.

## 2. 집 컴퓨터에서 설치할 것

집에서는 학원 PC에 설치된 내용이 자동으로 따라가지 않습니다.
집 컴퓨터에서 Codex와 Claude를 설치한 뒤 아래 명령을 다시 실행해야 합니다.

## 3. 집 컴퓨터 준비 순서

### 1단계: Node.js 설치

아래 사이트에서 LTS 버전을 설치합니다.

```text
https://nodejs.org
```

설치 후 PowerShell을 새로 열고 확인:

```powershell
node -v
npx -v
```

버전 번호가 나오면 성공입니다.

### 2단계: Codex 설치 및 로그인

집에서 Codex를 설치하고 로그인합니다.
설치 방법은 사용하는 Codex 안내 화면 기준으로 진행합니다.

### 3단계: Claude 설치 및 로그인

Claude Code 또는 Claude Desktop/CLI를 설치하고 로그인합니다.
Claude에서도 같은 skills를 쓰고 싶다면 아래 설치 명령을 실행합니다.

## 4. 집에서 실행할 설치 명령

PowerShell에서 실행합니다.

### find-skills 설치

```powershell
npx.cmd skills add vercel-labs/skills@find-skills
```

만약 위 명령이 실패하면 아래 방식으로 다시 요청합니다.

```text
Codex에게 말하기:
https://github.com/vercel-labs/skills 이 skill 설치해줘
```

### Ponytail 설치

```powershell
npx.cmd skills add dietrichgebert/ponytail@ponytail
```

Ponytail은 AI가 코드를 너무 길고 복잡하게 만들지 않도록 도와주는 skill입니다.

### Matt Pocock skills 설치

```powershell
npx.cmd skills@latest add mattpocock/skills
```

설치 중 선택 화면이 나오면 필요한 에이전트로 Codex와 Claude를 선택합니다.
자동 설치로 진행되면 여러 skill이 한 번에 설치됩니다.

중요:
Matt Pocock skills는 설치 후 `/setup-matt-pocock-skills`를 한 번 실행하라고 안내될 수 있습니다.
그때는 Codex 또는 Claude에게 아래처럼 말하면 됩니다.

```text
/setup-matt-pocock-skills 실행 도와줘. 나는 초보자야.
```

## 5. 집에서 Codex에게 처음 말할 문장

집에서 새로 시작하면 Codex에게 이렇게 말합니다.

```text
나는 학원에서 작업하던 초보자야.
이 폴더의 수업_인수인계.md를 읽고,
집 컴퓨터에서 필요한 설치와 설정을 순서대로 도와줘.
Codex와 Claude를 둘 다 사용할 예정이야.
```

## 6. 수업끝 업데이트 규칙

내가 Codex에게 `수업끝`이라고 말하면, Codex는 이 문서를 업데이트해야 합니다.

업데이트할 내용:

- 오늘 추가로 설치한 프로그램
- 오늘 추가로 설치한 skill
- 오늘 수정한 프로젝트 파일
- 집에서 이어서 해야 할 일
- 초보자 기준으로 조심해야 할 점

이 문서는 계속 최신 상태로 유지합니다.

## 7. 현재 프로젝트 폴더

현재 수업 폴더:

```powershell
D:\ai_바이브코딩\Yoom\수업18일차_코덱스로실습
```

현재 폴더에 있던 주요 파일:

- `index.html`
- `PRD.md`
- `AGENTS.md`
- `CLAUDE.md`
- `수업_인수인계.md`
