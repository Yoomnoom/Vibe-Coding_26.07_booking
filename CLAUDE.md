# Claude 작업 안내

## 프로젝트 개요

- 베이킹 클래스 예약 관리 서비스 실습 프로젝트입니다.
- `PRD.md`를 기준으로 작업합니다.
- 손님은 예약 문의를 남기고, 사장은 로그인 후 조회, 확정, 삭제합니다.
- 손님용 화면은 `index.html`, 사장용 화면은 `admin.html`입니다.

## 기술 규칙 (항상 지킬 것)

- HTML, CSS, JavaScript를 우선 사용합니다.
- 요청 없이 프레임워크나 라이브러리를 추가하지 않습니다.
- Supabase를 쓰면 예약 테이블에 RLS를 켭니다.
- 익명 사용자는 등록만 가능해야 합니다.
- 브라우저 코드에 `service_role` 키를 절대 넣지 않습니다.

## 범위 규칙 (절대 만들지 말 것)

- 고객 회원가입을 만들지 않습니다.
- 관리자 가입 화면을 만들지 않습니다.
- 결제 기능을 만들지 않습니다.
- 캘린더 자동 연동을 만들지 않습니다.
- 인스타그램 DM 자동 연동을 만들지 않습니다.
- PRD에 없는 기능은 먼저 사용자에게 물어봅니다.

## 브랜드 규칙

- 따뜻하고 부담 없는 원데이 베이킹 클래스 느낌을 유지합니다.
- 화면 문구는 짧고 친절한 한국어로 씁니다.
- 대표 문구는 `PRD.md`의 후보를 우선 사용합니다.

## 코드 스타일

- 들여쓰기는 2칸입니다.
- 파일 이름은 소문자로 씁니다.
- 클래스와 아이디는 의미가 보이게 씁니다.
- 필요할 때만 `styles.css`, `app.js`, `admin.js`를 추가합니다.

## 대화 규칙

- 사용자는 초보자입니다.
- 쉬운 한국어로 설명합니다.
- 명령어는 실행 이유를 먼저 설명합니다.
- 설치와 설정은 한 단계씩 안내합니다.

## 완료 전 확인

- 손님 예약 문의가 저장되는가
- 로그인 전 관리자 데이터가 보이지 않는가
- 사장이 조회, 확정, 삭제를 할 수 있는가
- 로그아웃하면 관리자 데이터가 숨겨지는가

