# O O 베이킹 클래스 예약 관리 서비스 PRD

## 서비스 이름

O O 베이킹 클래스 예약 관리 서비스

## 한 줄 소개

주말 원데이 베이킹 클래스를 소개하고, 손님이 예약 문의를 남기면 사장이 관리자 화면에서 접수 내역을 확인, 확정, 삭제할 수 있는 서비스입니다.

## 대표 문구 후보

1. 주말에 만나는 달콤한 원데이 베이킹 클래스
2. 처음이어도 괜찮은, 따뜻한 주말 베이킹 시간
3. 직접 만들고 맛보는 나만의 베이킹 클래스

## 핵심 기능

1. 서비스 소개: 클래스 설명, 가격, 위치를 보여줍니다.
2. 예약 문의 접수: 이름, 연락처, 희망 날짜, 요청사항을 입력받아 저장합니다.
3. 관리자 조회: 사장만 로그인해서 접수 내역을 최신순으로 봅니다.
4. 접수 상태 변경: 사장이 상태를 `대기`에서 `확정`으로 바꿉니다.
5. 접수 삭제: 사장이 스팸 또는 취소 접수를 삭제합니다.

## 화면 구성

### 고객 페이지 (`index.html`)

- 상단: 대표 문구와 서비스 한 줄 소개
- 중간: 소개 카드 3개
  - 무엇을 배우는지
  - 가격이 얼마인지
  - 어디에서 진행하는지
- 하단: 예약 문의 폼
  - 이름
  - 연락처
  - 희망 날짜
  - 요청사항
  - 접수 버튼

### 관리자 화면 (`admin.html`)

- 로그인 폼
  - 이메일
  - 비밀번호
- 로그인 후 접수 내역 표 표시
- 각 접수 행에 `확정`, `삭제` 버튼 표시
- 상태 표시
  - 대기: 회색
  - 확정: 초록
- 로그아웃 버튼 표시

## 데이터

저장할 항목:

- 이름: 텍스트, 필수
- 연락처: 텍스트, 필수
- 희망 날짜: 날짜, 필수
- 요청사항: 텍스트, 선택
- 상태: 텍스트, 기본값 `대기`
- 접수 시각: 자동 저장

상태 값:

- `대기`
- `확정`

## 권한 설계

손님 권한:

- 로그인 없이 예약 문의를 남길 수 있습니다.
- 접수 내역을 조회할 수 없습니다.
- 접수 상태를 바꿀 수 없습니다.
- 접수를 삭제할 수 없습니다.

사장 권한:

- 로그인 후 접수 내역을 조회할 수 있습니다.
- 접수 상태를 `확정`으로 바꿀 수 있습니다.
- 스팸 또는 취소 접수를 삭제할 수 있습니다.

Supabase 권한 기준:

- 예약 문의 테이블은 RLS를 켭니다.
- 익명 사용자(`anon`)는 `insert`만 가능하게 합니다.
- 익명 사용자(`anon`)는 `select`, `update`, `delete`를 할 수 없게 합니다.
- 로그인 사용자(`authenticated`) 중 사장 계정만 `select`, `update`, `delete`를 할 수 있게 합니다.
- 관리자 계정은 Supabase 화면에서 직접 1개만 만듭니다.
- 브라우저 코드에는 `service_role` 키를 절대 넣지 않습니다.

관리자 화면 기준:

- 로그인하지 않은 사용자는 접수 내역을 볼 수 없습니다.
- 로그인하지 않은 사용자가 `admin.html`에 들어와도 데이터가 표시되지 않아야 합니다.
- 로그아웃하면 접수 내역을 다시 숨깁니다.

## 만들지 않는 것

- 고객 회원가입은 만들지 않습니다.
- 관리자 가입 기능은 만들지 않습니다.
- 결제 기능은 만들지 않습니다.

## 검토 메모

기능은 늘리지 않고, 기존 범위 안에서 권한 기준을 보완했습니다.

중요한 허점:

- 손님이 다른 손님의 예약 문의를 조회하면 안 됩니다.
- 손님이 상태를 직접 `확정`으로 바꾸면 안 됩니다.
- 손님이 다른 사람의 접수를 삭제하면 안 됩니다.
- Supabase `service_role` 키가 프론트엔드에 노출되면 모든 권한이 뚫릴 수 있습니다.

